plugin: aws_ec2
regions:
  - ap-south-1
# Set the strict flag to false so it won't fail if some tags are missing
strict: False
# Groups hosts by the Environment tag (e.g., tag_Environment_dev)
keyed_groups:
  - key: tags.Environment
    prefix: tag_Environment
  - key: tags.Role
    prefix: tag_Role

# Force Ansible to use the Public IP address instead of the unreachable Private DNS
compose:
  ansible_host: public_ip_address
